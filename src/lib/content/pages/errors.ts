import { PageContent } from '../types';

export const errorsPage: PageContent = {
  route: "/errors",
  slug: "errors",
  pageJob: "Convert awareness to trust",
  sections: [],
  internalNotes: ["Status: shell-placeholder"]
};
