import React from 'react';

export interface MetricProps {
  value: string; label: string; note?: string;
}

export function Metric(props: MetricProps) {
  return (
    <div className="metric-component">
      {/* TODO: Implement Metric UI using tokens */}
      
      
      
    </div>
  );
}
